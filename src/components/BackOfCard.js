function BackOfCard() {
    return (
      <div className="absolute inset-0 w-full h-full flex justify-center items-center bg-green-100 text-gray-800 transition-all z-10 card-back">
        <p>
        You share your vision. All mission-sensitive information is protected by an NDA.
We assign a dedicated Business Development Manager to your project.
        </p>
      </div>
    );
  }

export default BackOfCard;